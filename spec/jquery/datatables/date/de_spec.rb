RSpec.describe Jquery::Datatables::Date::De do
  it "has a version number" do
    expect(Jquery::Datatables::Date::De::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
